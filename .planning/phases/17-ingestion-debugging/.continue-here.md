---
phase: 17-ingestion-debugging
task: 0
total_tasks: 0
status: in_progress
last_updated: 2026-03-02T15:13:25.002Z
---

<current_state>
We are mid-way through /gsd:discuss-phase 17. Phase 17 context has NOT been written yet — we were in the gray-area discussion loop when the session ended. No CONTEXT.md exists. Roadmap, requirements, and milestone are all set (v1.4 was just initialized this session).
</current_state>

<completed_work>

- Milestone v1.4 "Vault Seeding & Pipeline Validation" initialized (PROJECT.md, REQUIREMENTS.md, ROADMAP.md, STATE.md all committed)
- Roadmap: Phases 17 (Ingestion Debugging), 18 (Enrichment Validation), 19 (Drafting & Strategist)
- /gsd:list-phase-assumptions 17 run — assumptions surfaced and key corrections received from user
- /gsd:discuss-phase 17 started — codebase scouted, gray areas identified, but discussion loop did not complete

</completed_work>

<remaining_work>

- Complete /gsd:discuss-phase 17 — answer the 3 open questions below, then write CONTEXT.md
- Then: /gsd:plan-phase 17

</remaining_work>

<decisions_made>

## v1.4 Milestone Decisions
- Ingestion target: ALWAYS `2B-new/700 Model Citizen/` — this is the canonical vault for all ingestion
- Promotion: Separate strategy from ingestion — content moves from `2B-new` → `model-citizen-vault` later (NOT Phase 17 scope)
- Sources in scope: Slack (MCP intelligence scanner), GoodLinks, YouTube. Outlook deferred to v1.5.
- GoodLinks notes = stubs in the intelligence sense — collect URL from SQLite, then fetch full article body from web

## Key Architecture Clarifications (from this session)
- TWO vault locations exist:
  - `2B-new/700 Model Citizen/` = ingestion target (intelligence tier, v1.3+)
  - `model-citizen-vault/` = publication destination (v1.1 pipeline, enriched/published notes)
- GoodLinks currently writes to WRONG vault (`model-citizen-vault/sources/goodlinks/`)
- `extract-article.mjs` already exists and works (Readability + linkedom + Turndown) — GoodLinks notes in old vault already have full article body text via this tool
- Old `scan-slack.sh` = REST API with Bot Token (writes to old vault)
- New `scan-slack-intelligence.sh` = MCP-based, calls `claude --command scan-slack-intelligence` (writes to `2B-new/sources/slack/`)
- Existing Slack notes in `2B-new/sources/slack/` from Feb/Mar show scanner has run before

</decisions_made>

<blockers>

## 3 Open Questions for discuss-phase 17 (answer these first in new session)

1. **GoodLinks routing**: It currently writes to old vault but already has full article text via extract-article.mjs. Should Phase 17:
   - (A) Reroute ingest-goodlinks.sh to write to `2B-new/sources/goodlinks/` going forward, leave old notes in place — OR
   - (B) Reroute + migrate existing old-vault goodlinks notes to `2B-new/` — OR
   - (C) Leave GoodLinks in old vault, focus Phase 17 on Slack + YouTube only

2. **Slack scanner**: Two scanners exist. Which is the source of truth for `2B-new/`?
   - Old `scan-slack.sh` (Slack Bot Token, REST, writes to old vault)
   - New `scan-slack-intelligence.sh` (MCP, Claude Code command, writes to `2B-new/sources/slack/`)
   - User implied the intelligence scanner is the right one — but needs confirmation

3. **YouTube**: Does it currently write to `2B-new/` or old vault? Is this a known state or needs investigation?

</blockers>

<context>
Phase 17 is fundamentally a "debug and fix" phase — the pipeline code exists but has never been run against real data end-to-end. The goal is to get content-rich notes flowing into `2B-new/` from all three sources.

Key insight from session: `extract-article.mjs` is the web extraction tool already used by GoodLinks in old vault — it should be reused for the new vault pipeline too. The "stub" problem is about where notes land (wrong vault) not about missing content extraction capability.

The discuss-phase flow was working but the AskUserQuestion multi-select wasn't registering user clicks, so we fell back to direct text questions. In the new session, use single-select AskUserQuestion instead of multi-select — it's more reliable.

After getting answers to the 3 open questions above, write CONTEXT.md covering:
- Vault routing decisions (which source writes where)
- GoodLinks: rerouting + extraction approach
- Slack: which scanner, MCP debugging approach
- YouTube: target vault + silent failure definition
- Promotion boundary (explicitly out of scope)
</context>

<next_action>
Start new session with: `/gsd:discuss-phase 17`

The workflow will detect no CONTEXT.md exists and proceed to discussion.

IMPORTANT: Immediately surface the 3 open questions from <blockers> above before running the full gray-area analysis — use them as the starting point since we already have assumptions context from this session.

Then write CONTEXT.md and proceed to `/gsd:plan-phase 17`.
</next_action>
