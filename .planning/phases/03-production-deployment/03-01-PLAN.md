---
phase: 03-production-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/
  - static/img/profile.jpg
  - config/_default/params.toml
autonomous: false

must_haves:
  truths:
    - "User visits https://athan-dial.github.io/ and sees Hugo Resume site load"
    - "User inspects DevTools and sees no 404 errors for CSS/JS/fonts"
    - "Open Graph meta tags reference a valid image URL"
    - "Site scores above 90 on Lighthouse performance"
    - "All sections render correctly on mobile viewport"
  artifacts:
    - path: "docs/index.html"
      provides: "Built Hugo Resume homepage"
      contains: "hugo-resume"
    - path: "docs/css/resume.css"
      provides: "Theme stylesheet"
    - path: "static/img/profile.jpg"
      provides: "OG image and profile photo"
  key_links:
    - from: "docs/index.html"
      to: "docs/css/resume.css"
      via: "link href"
      pattern: "css/resume\\.css"
---

<objective>
Deploy the Hugo Resume site to GitHub Pages and validate it works in production.

Purpose: Complete the Hugo Resume milestone by shipping the built site live.
Output: Working production site at https://athan-dial.github.io/ with no 404s, valid OG tags, and mobile responsiveness confirmed.
</objective>

<execution_context>
@/Users/adial/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adial/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean build, fix OG image, commit and push</name>
  <files>docs/, static/img/profile.jpg, config/_default/params.toml</files>
  <action>
    1. Check if a profile image exists anywhere in the repo (static/images/, static/img/, etc.). The OG tag references `/img/profile.jpg` which currently 404s.
       - If no profile image exists: create a simple placeholder. Use a solid-color SVG converted to JPG, or find if the theme has a default. The key requirement is that `static/img/profile.jpg` exists so OG tags don't 404.
       - If a profile image exists elsewhere: copy it to `static/img/profile.jpg`.

    2. Verify OG tags in the theme templates. Check `themes/` or Hugo Resume's baseof.html for og:image references. Ensure the `og:image` path matches where we placed the image. If the theme pulls from a config param, set it in params.toml (e.g., `profileImage = "img/profile.jpg"`).

    3. Clean rebuild: `rm -rf docs/ && hugo`

    4. Verify no broken asset references: check that all CSS, JS, and font URLs in docs/index.html resolve to files that exist in docs/.

    5. Commit all changes to docs/ and push to gh-pages branch:
       ```
       git add docs/ static/img/
       git commit -m "deploy: rebuild docs/ with Hugo Resume theme for production"
       git push origin gh-pages
       ```
  </action>
  <verify>
    - `hugo` completes without errors
    - `ls docs/css/resume.css` exists
    - `ls docs/img/profile.jpg` exists (OG image)
    - `git log -1 --oneline` shows the deploy commit
    - `git status` shows clean working tree for docs/
  </verify>
  <done>docs/ directory contains fresh Hugo Resume build, OG image exists, changes pushed to gh-pages</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Hugo Resume site deployed to GitHub Pages. Clean build of docs/ committed and pushed to gh-pages branch. OG image added to prevent 404.
  </what-built>
  <how-to-verify>
    1. Visit https://athan-dial.github.io/ -- confirm Hugo Resume theme loads (sidebar with name, navigation, content sections)
    2. Open DevTools > Network tab -- confirm NO 404 errors for CSS, JS, fonts, or images
    3. Open DevTools > Console -- confirm no JavaScript errors
    4. Run Lighthouse audit (DevTools > Lighthouse > Generate report) -- confirm Performance score > 90
    5. Test on mobile device (or DevTools responsive mode at 375px width):
       - Sidebar collapses to hamburger menu
       - All sections (About, Experience, Skills, Education) are readable
       - No horizontal scrolling
    6. Test OG preview: paste https://athan-dial.github.io/ into https://www.opengraph.xyz/ or LinkedIn post composer -- confirm title, description, and image appear
    7. Verify dark mode toggle works (if present in theme)
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- Production URL loads without errors
- Lighthouse performance > 90
- Mobile responsive confirmed
- OG preview card renders correctly
</verification>

<success_criteria>
- https://athan-dial.github.io/ serves Hugo Resume site
- No 404 errors in DevTools Network tab
- Lighthouse performance score > 90
- OG tags produce valid preview card
- Mobile layout works on 375px viewport
</success_criteria>

<output>
After completion, create `.planning/phases/03-production-deployment/03-01-SUMMARY.md`
</output>
