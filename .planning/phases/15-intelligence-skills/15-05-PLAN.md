---
phase: 15-intelligence-skills
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/commands/synthesize-draft.md
autonomous: true
requirements:
  - SYNTH-01
  - SYNTH-02
  - SYNTH-03

must_haves:
  truths:
    - "Synthesis skill clusters atoms by theme and produces a draft blog post in vault/drafts/"
    - "Draft body contains inline [[atom]] wikilink citations"
    - "source_atoms frontmatter lists all cited atoms"
    - "Draft introduces no claims, statistics, or examples not present in the source atoms"
    - "Voice placeholder TODO comment is present in the command prompt (Deep Research not yet available)"
  artifacts:
    - path: ".claude/commands/synthesize-draft.md"
      provides: "Claude Code command invoked as /synthesize-draft <theme-or-topic>"
      contains: "TODO: Voice"
  key_links:
    - from: "atoms/ notes with theme wikilinks"
      to: "draft body"
      via: "source_atoms frontmatter + inline [[atom]] citations in body"
      pattern: "source_atoms"
    - from: "draft note"
      to: "drafts/ folder"
      via: "tags: [type/draft] for ANM routing"
      pattern: "type/draft"
---

<objective>
Build the synthesis skill as a Claude Code command that clusters atoms by theme and generates a draft blog post with inline citations, written only from provided atom content.

Purpose: Synthesis closes the loop from raw captured content to publishable output — turning linked atoms into coherent narrative while maintaining full traceability to source material.

Output: .claude/commands/synthesize-draft.md (Claude Code command invoked as `/synthesize-draft <theme-or-topic>`). Draft notes written to vault/drafts/ on each invocation.

NOTE: Voice & Style Guide (ChatGPT Deep Research) is not yet available. The synthesis prompt carries a TODO placeholder for voice injection. The structural pipeline (clustering, citation, draft format) is complete and valuable without it.
</objective>

<execution_context>
@/Users/adial/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adial/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@model-citizen/vault/SCHEMA.md
@.planning/phases/15-intelligence-skills/15-RESEARCH.md

<interfaces>
<!-- Draft note frontmatter from Phase 14 SCHEMA.md -->

Draft note frontmatter:
```yaml
---
title: "Why Adoption Curves Always Plateau"
type: draft
created: 2026-03-01
modified: 2026-03-01
content_status: draft
publishability: private
source_atoms:
  - "[[Adoption curves have a predictable plateau at ~40% active users]]"
  - "[[User behavior changes at the 40% adoption threshold]]"
tags: [type/draft]
---

[Draft body with inline [[atom-title]] citations]
```

Required frontmatter fields for draft:
- title: Blog post title
- type: draft (always)
- created: Today's date (ISO 8601)
- modified: Today's date (ISO 8601)
- content_status: draft (always — newly created)
- publishability: private (always — requires manual flip to publish)
- source_atoms: list of wikilinks to all atoms cited in the body
- tags: [type/draft] (required for ANM routing)

Vault paths:
- atoms/: ~/Library/Mobile Documents/iCloud~md~obsidian/Documents/2B-new/700 Model Citizen/atoms/
- themes/: ~/Library/Mobile Documents/iCloud~md~obsidian/Documents/2B-new/700 Model Citizen/themes/
- drafts/: ~/Library/Mobile Documents/iCloud~md~obsidian/Documents/2B-new/700 Model Citizen/drafts/
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write synthesize-draft command</name>
  <files>
    .claude/commands/synthesize-draft.md
  </files>
  <action>
Write `.claude/commands/synthesize-draft.md` — a Claude Code command that takes a theme name or topic as input and produces a draft blog post.

The command accepts one argument: a theme name or topic string. Example invocations:
- `/synthesize-draft "User adoption psychology"`
- `/synthesize-draft "product-market fit signals"`

The command prompt must instruct Claude to:

1. Accept the theme name / topic as input.

2. Find relevant atoms using two passes:
   a. **Theme tag grep**: grep `atoms/*.md` for atoms that have a wikilink to the given theme name in their `## Theme connections` section: `grep -li "[[{theme}]]" atoms/*.md`
   b. **Direct tag grep**: grep `atoms/*.md` for atoms whose tags array includes a tag matching the theme name (case-insensitive)
   Merge results. Deduplicate. If fewer than 2 atoms found, report "insufficient atoms for synthesis — run /match-themes on more atoms first" and stop.

3. Read all matching atoms. For each atom, load:
   - Full title
   - Full body content (the concept + why-it-matters sentences)
   - split_index (for ordering, if useful)

4. Generate the draft blog post:
   - Derive a post title from the theme + the strongest through-line across atoms
   - Write an intro paragraph that frames the question or problem
   - Write 2-4 body paragraphs, each grounded in one or more atoms
   - Include inline `[[atom-title]]` wikilinks as citations wherever a claim derives from an atom
   - Write a closing paragraph that synthesizes the key takeaway

   CRITICAL SYNTH-03 constraint — embed this exact instruction in the prompt:
   "Write ONLY from the atom content provided in this prompt. Do not add examples, statistics, or claims not present in the atoms. Every paragraph must be traceable to at least one cited atom. If you cannot support a paragraph with an atom citation, do not write that paragraph. Do not generalize beyond what the atoms state."

   VOICE PLACEHOLDER — embed this block in the prompt:
   ```
   # TODO: Voice & Style Guide
   # ChatGPT Deep Research Voice & Style Guide is not yet available.
   # When available, inject Athan's signature vocabulary and sentence patterns here.
   # For now: write in clear, direct prose. No hedging. No passive voice. No "I helped..." constructions.
   # Favor declarative sentences. State the claim, then support it.
   ```

5. Build `source_atoms` list: collect wikilinks for all atoms that were actually cited in the body (not all atoms found — only those referenced inline).

6. Write draft note to `~/Library/Mobile Documents/iCloud~md~obsidian/Documents/2B-new/700 Model Citizen/drafts/{sanitized-title}.md` with SCHEMA.md draft frontmatter (see interfaces above).

7. Report: Draft title, N atoms synthesized, draft path, reminder that publishability must be manually flipped to public before the draft can enter the publish pipeline.

CRITICAL constraints to embed in the command prompt:
- Write draft directly to `drafts/` folder — NOT to vault root
- Draft MUST have `tags: [type/draft]` for ANM routing
- publishability is always `private` — the human approval gate (SYNTH-04) requires manual promotion
- source_atoms must list only atoms actually cited in the body — not all matched atoms
- If 0 atoms found for the theme, report the gap and stop — do not generate an uncited draft
- Filename: sanitize title, lowercase-hyphenated-slug, truncate at 60 chars
  </action>
  <verify>
    <automated>test -f /Users/adial/Documents/GitHub/athan-dial.github.io/.claude/commands/synthesize-draft.md && grep -c "TODO: Voice" /Users/adial/Documents/GitHub/athan-dial.github.io/.claude/commands/synthesize-draft.md</automated>
  </verify>
  <done>synthesize-draft.md command exists. SYNTH-03 "write only from atoms" constraint is embedded verbatim. Voice TODO placeholder is present. Draft writes to drafts/ with type: draft + tags: [type/draft]. publishability defaults to private with explicit human approval note.</done>
</task>

</tasks>

<verification>
1. `.claude/commands/synthesize-draft.md` exists
2. Command prompt contains SYNTH-03 constraint: "Write ONLY from the atom content provided"
3. Command prompt contains TODO: Voice placeholder comment (pending Deep Research)
4. Command writes to `drafts/` directly (not vault root)
5. Draft has type: draft + tags: [type/draft] for ANM routing
6. source_atoms lists only cited atoms (not all matched atoms)
7. publishability defaults to private with manual flip requirement noted
8. Command reports if insufficient atoms found (fewer than 2) rather than generating empty draft
</verification>

<success_criteria>
- Running `/synthesize-draft "user adoption"` finds related atoms and produces a draft blog post in vault/drafts/
- Draft body contains inline [[atom]] citations for all claims
- source_atoms frontmatter lists every atom cited
- Draft contains no claims not traceable to a source atom
- publishability is private (requires manual flip to publish)
- Voice TODO placeholder is present and clearly marked for future injection
</success_criteria>

<output>
After completion, create `.planning/phases/15-intelligence-skills/15-05-SUMMARY.md`
</output>
