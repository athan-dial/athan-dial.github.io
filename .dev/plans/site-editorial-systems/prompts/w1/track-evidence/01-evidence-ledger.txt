REPO: /Users/adial/GitHub/athan-dial.github.io  (Hugo 0.154.3+extended, themeless, branch gh-pages)

READ FIRST (in this order):
  1. CLAUDE.md — repo non-negotiables. NOTE: its "Clinical Architect" design section is STALE and
     is being replaced by this work. Its content-authenticity and employer-safety rules still apply.
  2. .planning/design/EDITORIAL-SYSTEMS.md — canonical design tokens. Source of truth.
  3. .planning/design/REDESIGN-PLAN.md — the strategy spec driving all of this.

NON-NEGOTIABLES — read every line, these are load-bearing:
  * NEVER run `rm -rf docs/`, `rm -rf docs`, or any command that clears the publishDir.
    docs/agency/** is 127 tracked files with NO SOURCE anywhere — not in this repo, not in
    athan-dial/skills. A clean Hugo build does NOT regenerate it. Deleting it destroys the live
    Agency site permanently. Same caution for docs/skills/** (regenerable only via
    scripts/fetch-skills.sh, which needs network + gh auth).
  * Build to a throwaway dir when checking your work: `hugo --gc --destination /private/tmp/hugo-guard`.
    Never `hugo` bare (it writes docs/).
  * NO dark mode. Editorial Systems is light-only; the html.dark block and the theme toggle are
    being removed. Do not add dark variants, do not add `prefers-color-scheme` color blocks.
  * NO shadows. NO border radius (0px everywhere). NO third-party embeds or iframes (the LinkedIn
    platform script and badge are being removed).
  * The Stitch screen HTML in .planning/design/stitch-screens/*.html is a STRUCTURAL AND VISUAL
    REFERENCE ONLY. Its copy is FABRICATED — the About screen invents "Head of Product, Synthesis",
    "QuantCo", and "PhD Researcher, MIT", none of which are real. Never lift prose, job titles,
    employers, dates, or metrics from those files. Take layout, hierarchy, rhythm, and component
    structure only.
  * Do NOT write new factual claims about Athan's work. If a template needs example content, use
    obviously-marked placeholder text or render from data/profile.toml.
  * Do NOT commit. Do NOT push. Leave changes in the working tree. Minimal focused diff — touch
    only the files this brief names.

GUARD — run before reporting done, must pass:
  hugo --gc --destination /private/tmp/hugo-guard && test -f docs/agency/index.html && test -d docs/skills

REPORT: list files changed, what the guard printed, and anything you could not do.

WAVE 1 CONTEXT: Wave 0 has already landed. assets/css/tokens.css exists with the full Editorial
Systems token layer and assets/css/main.css implements the grid, type scale, rules, and components.
CONSUME those tokens — do not define new colors, sizes, or spacing values, and do not add a <style>
block. If you genuinely need a token that does not exist, add it to tokens.css only if this brief
says you own that file; otherwise report it as a gap. data/profile.toml is the single source for
name, role, employer, location, email, links, and the positioning lines — read from it, never
hardcode those strings. Other tracks are editing other files in parallel: touch ONLY the files this
brief names.

TASK: Build the evidence ledger and rank the work-story candidates. This is research and
classification — you write documentation, not site content.

WHY THIS PR EXISTS: The plan wants two evidence-validated case studies at launch. Nobody has verified
which stories have publishable evidence. The plan names two candidates (a 40-plus-day sourcing cycle
moving toward a 14-day operating target; making a three-billion-compound search space usable) but calls
them "working directions, not publication-ready claims". Meanwhile CLAUDE.md forbids writing case
studies without factual grounding, and the ChatGPT Deep Research that was supposed to supply it NEVER
LANDED — "Voice and Style Guide.md" and "Montai Work Archaeology.md" exist only as unfilled prompt
stubs in the Obsidian vault trash. So the evidence has to be assembled from what actually exists.
Athan reads your output and picks two stories. Drafting happens in a LATER plan, not here.

READ FIRST:
  * .planning/design/REDESIGN-PLAN.md — sections "The Work section should prove the thesis" (the
    evidence and employer-safety gate, and the seven things the gate must verify) and "The brand should
    have one center and several proofs" (the three proof pillars). Both are your rubric.
  * CLAUDE.md — Content Authenticity Constraint and Employer Safety Guidelines. Binding.
  * content/case-studies/*.md — the seven quarantined studies. Treat these as UNRELIABLE: the repo
    itself labels them fabricated or generic. Mine them for candidate SUBJECTS only, never for facts.
  * content/about.md, content/resume.md, data/experience.json — the only in-repo biographical facts.
  * content/notes/*.md and docs/agency/dispatches/* — Athan's real practitioner voice.
  * .planning/PROJECT.md and .planning/milestones/* — project history.

CONTEXT PACK — assemble from these sources, and record in CONTEXT-PACK.md what each yielded:
  1. This repo, as above.
  2. Athan's durable memory. Two candidates were already mined and confidence-rated, and they are the
     only evidence-assessed stories that exist. They are recorded as IL-CAND-004 (HIGH confidence)
     and IL-CAND-005 (MEDIUM-HIGH). THIS REPO IS PUBLIC, so their subject matter — internal program
     names, predicted-vs-actual rates, and pilot status — is deliberately not restated here. Read
     the candidate detail from durable memory and from .planning/private/ (gitignored) instead.
     Retrieve the full notes via the basic-memory MCP (search_notes / read_note) and via
     /Users/adial/.claude/projects/-Users-adial-GitHub-athan-dial-github-io/memory/. If basic-memory is
     unreachable, SAY SO in your report — do not silently fall back.
  3. The Obsidian vault, READ-ONLY. Vault root is the 2B-new-icloud directory under the user's iCloud
     Obsidian documents folder; resolve it with a quoted shell variable, e.g.
       VAULT="$HOME/Library/Mobile Documents/iCloud~md~obsidian/Documents/2B-new-icloud"
     and always quote "$VAULT" in every command (the path contains spaces and a tilde).
     Specifically read "$VAULT/600 Resources/Voice Packets/tnfr1-strategy-voice-packet.md" (6.3KB, real),
     and search "$VAULT/200 Projects" and "$VAULT/08 Knowledge" for Montai work records.
     NEVER write into the vault. Never create a git repo, worktree, clone, or dependency tree anywhere
     inside the iCloud documents tree — it is iCloud-synced and high file counts stall the sync queue.
  4. Montai Confluence and Jira, READ-ONLY, for evidence on the two plan-named candidates (sourcing
     cycle time; compound search space). Use the twg-* skills for discovery and read
     (twg-agentic-search, twg-confluence, twg-jira) per the repo owner's Atlassian routing rules. Make
     NO writes to Jira, JPD, or Confluence. If auth is unavailable, record the gap rather than guessing.

DELIVERABLES:

1. .planning/CONTEXT-PACK.md (new) — the reusable source manifest for all later content work: every
   source consulted, its path or query, what it yielded, and its trust level. This is what the drafting
   plan will load instead of re-discovering everything. Include the exact retrieval commands.

2. .planning/EVIDENCE-LEDGER.md (new) — the claim-by-claim ledger. For EVERY factual claim available
   about Athan's work, one row: the claim, its source, and its classification using the plan's four
   levels — public-and-verified / publishable-in-a-range / publishable-as-mechanism-only / private.
   For each claim also answer the plan's seven gate questions explicitly: role title and dates;
   whether the metric is measured, estimated, or a target; whether the result is attributable to
   Athan's intervention; names of internal programs, partners, models, vendors; dollar figures and
   portfolio information; whether the outcome occurred or was only projected; whether the employer
   would consider it confidential.
   Resolve the role-title contradiction on the record: the repo variously says Associate Director, Data
   Research Lead, and Head of Decision Science & Product. State which is correct with its source, and
   flag any page still carrying a wrong one.

3. .planning/WORK-CANDIDATES.md (new) — ranked story candidates, best first. For each: working title,
   the proof pillar it demonstrates (product judgment / technical credibility / organizational
   leverage), the expert users, the product boundary it illustrates, which of the eight case-template
   sections you can already fill and which are empty, the strongest publishable claim and its
   classification, the employer-safety risk with a specific mitigation, and a blunt verdict:
   READY-TO-DRAFT / NEEDS-ONE-FACT (name the fact and where to get it) / NOT-VIABLE (say why).
   Cover at minimum: the two candidates named in the redesign plan, both IL-CAND memory
   candidates, and any others the sources surface.
   End with a clear recommendation of which two to draft and one shorter proof note, and say plainly if
   fewer than two are viable — that is a legitimate finding and far better than manufacturing a third.

HARD CONSTRAINTS:
  * Write NOTHING under content/. No case studies, no drafts, no prose for the site. Documentation only.
  * Invent no facts, no metrics, no dates. An empty cell with "no evidence found" is the correct output
    when there is no evidence. Every claim needs a retrievable source; mark inferences as inferences.
  * Read-only against Confluence, Jira, the vault, and basic-memory. No writes anywhere outside
    .planning/.
  * Employer safety is the binding constraint, not a consideration. When in doubt, classify DOWN.

Verify: guard passes (you changed no templates, so it should be untouched); all three files exist and
every claim row cites a retrievable source; `git status --short` shows changes only under .planning/.
