REPO: /Users/adial/GitHub/athan-dial.github.io  (Hugo 0.154.3+extended, themeless, branch gh-pages)

READ FIRST (in this order):
  1. CLAUDE.md — repo non-negotiables. NOTE: its "Clinical Architect" design section is STALE and
     is being replaced by this work. Its content-authenticity and employer-safety rules still apply.
  2. .planning/design/EDITORIAL-SYSTEMS.md — canonical design tokens. Source of truth.
  3. .planning/design/REDESIGN-PLAN.md — the strategy spec driving all of this.

NON-NEGOTIABLES — read every line, these are load-bearing:
  * NEVER run `rm -rf docs/`, `rm -rf docs`, or any command that clears the publishDir.
    docs/agency/** is 127 tracked files with NO SOURCE anywhere — not in this repo, not in
    athan-dial/skills. A clean Hugo build does NOT regenerate it. Deleting it destroys the live
    Agency site permanently. Same caution for docs/skills/** (regenerable only via
    scripts/fetch-skills.sh, which needs network + gh auth).
  * Build to a throwaway dir when checking your work: `hugo --gc --destination /private/tmp/hugo-guard`.
    Never `hugo` bare (it writes docs/).
  * NO dark mode. Editorial Systems is light-only; the html.dark block and the theme toggle are
    being removed. Do not add dark variants, do not add `prefers-color-scheme` color blocks.
  * NO shadows. NO border radius (0px everywhere). NO third-party embeds or iframes (the LinkedIn
    platform script and badge are being removed).
  * The Stitch screen HTML in .planning/design/stitch-screens/*.html is a STRUCTURAL AND VISUAL
    REFERENCE ONLY. Its copy is FABRICATED — the About screen invents "Head of Product, Synthesis",
    "QuantCo", and "PhD Researcher, MIT", none of which are real. Never lift prose, job titles,
    employers, dates, or metrics from those files. Take layout, hierarchy, rhythm, and component
    structure only.
  * Do NOT write new factual claims about Athan's work. If a template needs example content, use
    obviously-marked placeholder text or render from data/profile.toml.
  * Do NOT commit. Do NOT push. Leave changes in the working tree. Minimal focused diff — touch
    only the files this brief names.

GUARD — run before reporting done, must pass:
  hugo --gc --destination /private/tmp/hugo-guard && test -f docs/agency/index.html && test -d docs/skills

REPORT: list files changed, what the guard printed, and anything you could not do.

WAVE 1 CONTEXT: Wave 0 has already landed. assets/css/tokens.css exists with the full Editorial
Systems token layer and assets/css/main.css implements the grid, type scale, rules, and components.
CONSUME those tokens — do not define new colors, sizes, or spacing values, and do not add a <style>
block. If you genuinely need a token that does not exist, add it to tokens.css only if this brief
says you own that file; otherwise report it as a gap. data/profile.toml is the single source for
name, role, employer, location, email, links, and the positioning lines — read from it, never
hardcode those strings. Other tracks are editing other files in parallel: touch ONLY the files this
brief names.

TASK: The shared shell — navigation, footer, and a redirect map that keeps every retired URL alive.

READ FIRST: layouts/partials/nav.html, layouts/partials/footer.html, config/_default/menus.en.toml,
data/profile.toml, .planning/design/REDESIGN-PLAN.md section "The information architecture should be
obvious, not clever" (the nav table AND the URL plan table — both are requirements),
.planning/design/stitch-screens/home.html (header/nav and footer structure).

DELIVERABLES:

1. config/_default/menus.en.toml — the plan's primary navigation, exactly four items in this order:
   Work (/work/), Thinking (/thinking/), About (/about/), Résumé (/resume/). Remove "The Agency" and
   "Advisory" from primary nav — the plan says Agency stops being visible branding and Advisory is
   demoted into About#conversations. Keep the commented-out Case Studies entry deleted, not
   commented; /case-studies/ is being redirected.

2. layouts/partials/nav.html — rewrite for Editorial Systems. The wordmark is plain "Athan Dial"
   (from data/profile.toml wordmark) linking home; no "PhD" in it. Requirements:
     - NOT glassmorphic. No backdrop-filter, no blur, no shadow, no translucency. Editorial Systems
       is flat: a solid paper/surface bar with a 1px --rule bottom border.
     - REMOVE the theme-toggle button entirely. Wave 0 already deleted its JS listener, so the button
       is currently dead markup. Removing it is part of your job.
     - nav links in IBM Plex Mono label-mono or metadata-caps per the token layer; active-section
       state using structural-teal.
     - keyboard accessible with a visible focus outline; a real <nav> landmark with an aria-label;
       mobile behaviour that works without JS if possible, and with minimal vanilla JS if not
       (no framework, and honour prefers-reduced-motion).
     - pointer targets at least 44px.

3. layouts/partials/footer.html — rewrite. Per the plan's homepage section 6, a low-pressure
   conversation path: LinkedIn, email, and résumé, from data/profile.toml. NO consultancy funnel, no
   discovery-call language, no lead capture, no contact form. Flat, rule-separated, monospace labels.

4. data/redirects.toml (new) + layouts/_default/redirect.html (new) — the redirect map. The plan's URL
   table, every row:
     /case-studies/          -> /work/            (permanent)
     /case-studies/<slug>/   -> /work/            (each of the seven quarantined slugs)
     /agency/                -> /thinking/notes/
     /agency/dispatches/<slug>/ -> /notes/<slug>/ for the six notes migrated by the notes track; the
                                remaining Agency deep links must keep resolving — see the constraint
                                below
     /advisory/              -> /about/#conversations
     /consulting/            -> /about/#conversations   (an alias already exists for this)
     /writing/               -> /thinking/
   Implement with Hugo's redirect capability that does NOT require touching content frontmatter
   (other tracks own those files): a custom output format rendering meta-refresh + canonical link
   stubs from data/redirects.toml is the expected approach. Configure it in a way that does not
   collide with config edits from other tracks — if you must add an [outputs] block to
   config/_default/hugo.toml, DO NOT; instead report that you need it and implement everything else,
   because hugo.toml is owned by Wave 0 and edited by no Wave 1 track.

   HARD CONSTRAINT: docs/agency/** is a committed static tree with no source. Your redirect stubs
   must NOT overwrite or shadow any existing path under docs/agency/ that still needs to serve real
   content. Only the six migrated dispatch slugs redirect; playbooks and the rest stay live. Verify
   this — build to /private/tmp/hugo-guard and confirm you have not generated a file that would
   land on top of a committed docs/agency path.

5. scripts/verify-redirects.sh (new, executable) — the automated redirect test the plan asks for
   ("Add a redirect map and automated test so the redesign does not break old Agency, case-study,
   resume, writing, or advisory links"). Given a built site directory, assert every source route in
   data/redirects.toml resolves to a file containing the expected target, and that a hardcoded list
   of must-not-break paths still exists: /agency/playbooks/build-your-first-skill/index.html,
   /agency/dispatches/index.html, /skills/, /resume.pdf, /about/index.html. Exit non-zero naming the
   failure. No network.

Verify: guard passes, scripts/verify-redirects.sh passes against /private/tmp/hugo-guard, and nav
renders four items with no theme toggle and no backdrop-filter anywhere in the built CSS/HTML.
