REPO: /Users/adial/GitHub/athan-dial.github.io  (Hugo 0.154.3+extended, themeless, branch gh-pages)

READ FIRST (in this order):
  1. CLAUDE.md — repo non-negotiables. NOTE: its "Clinical Architect" design section is STALE and
     is being replaced by this work. Its content-authenticity and employer-safety rules still apply.
  2. .planning/design/EDITORIAL-SYSTEMS.md — canonical design tokens. Source of truth.
  3. .planning/design/REDESIGN-PLAN.md — the strategy spec driving all of this.

NON-NEGOTIABLES — read every line, these are load-bearing:
  * NEVER run `rm -rf docs/`, `rm -rf docs`, or any command that clears the publishDir.
    docs/agency/** is 127 tracked files with NO SOURCE anywhere — not in this repo, not in
    athan-dial/skills. A clean Hugo build does NOT regenerate it. Deleting it destroys the live
    Agency site permanently. Same caution for docs/skills/** (regenerable only via
    scripts/fetch-skills.sh, which needs network + gh auth).
  * Build to a throwaway dir when checking your work: `hugo --gc --destination /private/tmp/hugo-guard`.
    Never `hugo` bare (it writes docs/).
  * NO dark mode. Editorial Systems is light-only; the html.dark block and the theme toggle are
    being removed. Do not add dark variants, do not add `prefers-color-scheme` color blocks.
  * NO shadows. NO border radius (0px everywhere). NO third-party embeds or iframes (the LinkedIn
    platform script and badge are being removed).
  * The Stitch screen HTML in .planning/design/stitch-screens/*.html is a STRUCTURAL AND VISUAL
    REFERENCE ONLY. Its copy is FABRICATED — the About screen invents "Head of Product, Synthesis",
    "QuantCo", and "PhD Researcher, MIT", none of which are real. Never lift prose, job titles,
    employers, dates, or metrics from those files. Take layout, hierarchy, rhythm, and component
    structure only.
  * Do NOT write new factual claims about Athan's work. If a template needs example content, use
    obviously-marked placeholder text or render from data/profile.toml.
  * Do NOT commit. Do NOT push. Leave changes in the working tree. Minimal focused diff — touch
    only the files this brief names.

GUARD — run before reporting done, must pass:
  hugo --gc --destination /private/tmp/hugo-guard && test -f docs/agency/index.html && test -d docs/skills

REPORT: list files changed, what the guard printed, and anything you could not do.

TASK: Replace the HARBOR design system with Editorial Systems. This is the shared shim every Wave 1
template consumes — get the tokens right and the rest is composition.

READ FIRST: .planning/design/EDITORIAL-SYSTEMS.md (all of it, it is the token contract),
assets/css/main.css (current HARBOR system, ~37KB, the thing you are replacing),
assets/css/custom.css, layouts/_default/baseof.html,
.planning/design/stitch-screens/home.html (for component structure only — its Tailwind classes
encode the intended spacing and rule usage; read them, do not copy them).

WHAT HARBOR IS AND WHY IT GOES: slate-paper #f1f5f9 with deep-water #0c4a6e, shadow-heavy
(--shadow-card, --shadow-portrait, --shadow-xl), rounded, thin-Manrope marketing type, with a full
html.dark block. Editorial Systems is its opposite: warm paper, ink, strictly flat, 0px corners,
serif thesis headings, light-only. Do not try to preserve HARBOR's shadow or radius tokens.

DELIVERABLES:

1. assets/css/tokens.css (new) — the complete Editorial Systems token layer as CSS custom properties
   on :root. Every color, type style, and spacing value from EDITORIAL-SYSTEMS.md, named exactly as
   the token table names them (--paper, --surface, --ink, --ink-secondary, --structural-teal,
   --evidence-amber, --rule, plus the surface ladder, plus --max-width, --reading-column, --gutter,
   --margin-mobile, --margin-desktop, --stack-sm/md/lg/xl). Include the type scale as either custom
   properties or documented utility classes — your call, but be consistent and comment it.
   NO html.dark block. NO shadow tokens. NO radius tokens (or a single --radius: 0 if that reads
   better).

2. assets/css/main.css (rewrite) — the Editorial Systems implementation, importing/consuming
   tokens.css. Must deliver:
     - @font-face for the three families, self-hosted (see 4 below)
     - a 12-column grid utility with 1240px max shell, 720px reading column, and a section-rail
       pattern (outer column for monospaced margin notes / evidence labels)
     - base typography: body-md 17px/28px Manrope, body-lg 19px/32px, 65–75ch measure on prose
     - Source Serif 4 heading scale per the table, including the mobile display step-down
     - IBM Plex Mono metadata-caps (12px, 500, 0.08em, uppercase) and label-mono
     - rules: 1px --rule dividers, 2–3px for major section anchors
     - components per EDITORIAL-SYSTEMS.md: buttons (ink bg / paper text primary; 1px rule border
       secondary; structural-teal active), evidence labels (amber on light amber tint), grouped-field
       lists (rows separated by rules, NOT cards), square inputs with 2px structural-teal focus
     - hover = background shift paper→surface or underline reveal. Never a shadow.
     - visible focus outline on every interactive element, high contrast, per WCAG 2.2 focus-appearance
       (the plan links https://www.w3.org/WAI/WCAG22/Understanding/focus-appearance.html) — a clear
       outline, not a subtle glow
     - @media (prefers-reduced-motion: reduce) disabling all transitions
     - mobile <768px: single column, 16px margins, monospace metadata ABOVE primary content
     - zero !important
   Keep assets/css/custom.css only if something still needs it; if it is HARBOR-specific leftovers,
   reduce it to nothing rather than deleting the file, and say so.

3. layouts/_default/baseof.html (modify):
     - REMOVE the dark-mode bootstrap script in <head> and the theme-toggle listener script at the
       end of <body>. There will be no toggle. (layouts/partials/nav.html currently renders the
       toggle button — a different PR owns that file; the listener referencing
       getElementById('theme-toggle') must go regardless, and note in your report that nav.html
       still has the button so the shell track removes it.)
     - REMOVE the LinkedIn platform script (platform.linkedin.com/badges/js/profile.js). No embeds.
     - REMOVE the Google Fonts <link> and both fonts.googleapis/gstatic preconnects. Fonts are
       self-hosted now; add <link rel="preload"> for the woff2 files instead.
     - wire in assets/css/tokens.css alongside main.css through Hugo Pipes with the same
       minify+fingerprint+integrity treatment the existing main.css link uses. Concatenating them
       into one fingerprinted bundle is preferable — do that if it is clean.
     - add {{ partial "schema.html" . }} and {{ partial "og-image.html" . }} calls in <head>.
     - keep the skip-link, keep <main id="main-content">, keep partialCached favicons.

4. static/fonts/ — self-host the three families. CRITICAL EXISTING BUG: main.css already declares
   @font-face src url('/fonts/manrope-latin.woff2') but static/fonts/ DOES NOT EXIST, so the
   self-hosted font 404s today and silently falls back. Fix it for real:
     - fetch latin-subset woff2 for Manrope (variable 200–800), Source Serif 4 (variable, 400–700,
       need italic too for editorial pull-quotes), IBM Plex Mono (400, 500) from fonts.gstatic.com
     - place them in static/fonts/ with the exact filenames your @font-face rules reference
     - write static/fonts/README.md (new) recording the source URL, subset range, and the exact
       command used, so this is reproducible
     - set font-display: swap and correct unicode-range for the latin subset
   If network access is unavailable in your environment: still write the @font-face rules and the
   README with the exact fetch commands, add the Google Fonts <link> back as an explicitly-commented
   TEMPORARY fallback so the site is not unstyled, and report the failure loudly. Do NOT leave a
   404ing @font-face with no fallback — that is the bug you are fixing.

5. layouts/partials/schema.html (new) and layouts/partials/og-image.html (new) — STUBS ONLY.
   Each should be a valid, no-op-safe partial with a comment saying the meta track (w1-meta-01)
   implements it. They exist so baseof.html has something to call. Do not implement structured data
   here; that PR owns it.

Verify: guard passes, the `_build` deprecation warning is gone or unchanged (w0-02 owns that), the
built CSS in /private/tmp/hugo-guard/css/ contains the Editorial Systems tokens, no rule anywhere
sets a box-shadow or a non-zero border-radius, and grep the built HTML for "linkedin.com/badges" and
"fonts.googleapis" returning nothing.
