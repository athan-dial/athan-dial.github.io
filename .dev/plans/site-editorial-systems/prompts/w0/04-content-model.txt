REPO: /Users/adial/GitHub/athan-dial.github.io  (Hugo 0.154.3+extended, themeless, branch gh-pages)

READ FIRST (in this order):
  1. CLAUDE.md — repo non-negotiables. NOTE: its "Clinical Architect" design section is STALE and
     is being replaced by this work. Its content-authenticity and employer-safety rules still apply.
  2. .planning/design/EDITORIAL-SYSTEMS.md — canonical design tokens. Source of truth.
  3. .planning/design/REDESIGN-PLAN.md — the strategy spec driving all of this.

NON-NEGOTIABLES — read every line, these are load-bearing:
  * NEVER run `rm -rf docs/`, `rm -rf docs`, or any command that clears the publishDir.
    docs/agency/** is 127 tracked files with NO SOURCE anywhere — not in this repo, not in
    athan-dial/skills. A clean Hugo build does NOT regenerate it. Deleting it destroys the live
    Agency site permanently. Same caution for docs/skills/** (regenerable only via
    scripts/fetch-skills.sh, which needs network + gh auth).
  * Build to a throwaway dir when checking your work: `hugo --gc --destination /private/tmp/hugo-guard`.
    Never `hugo` bare (it writes docs/).
  * NO dark mode. Editorial Systems is light-only; the html.dark block and the theme toggle are
    being removed. Do not add dark variants, do not add `prefers-color-scheme` color blocks.
  * NO shadows. NO border radius (0px everywhere). NO third-party embeds or iframes (the LinkedIn
    platform script and badge are being removed).
  * The Stitch screen HTML in .planning/design/stitch-screens/*.html is a STRUCTURAL AND VISUAL
    REFERENCE ONLY. Its copy is FABRICATED — the About screen invents "Head of Product, Synthesis",
    "QuantCo", and "PhD Researcher, MIT", none of which are real. Never lift prose, job titles,
    employers, dates, or metrics from those files. Take layout, hierarchy, rhythm, and component
    structure only.
  * Do NOT write new factual claims about Athan's work. If a template needs example content, use
    obviously-marked placeholder text or render from data/profile.toml.
  * Do NOT commit. Do NOT push. Leave changes in the working tree. Minimal focused diff — touch
    only the files this brief names.

GUARD — run before reporting done, must pass:
  hugo --gc --destination /private/tmp/hugo-guard && test -f docs/agency/index.html && test -d docs/skills

REPORT: list files changed, what the guard printed, and anything you could not do.

TASK: Create the content model — collections, frontmatter contract, and the build rule that keeps
unvalidated content off the public site.

READ FIRST: config/_default/hugo.toml, .planning/design/REDESIGN-PLAN.md sections "The technical
rebuild should simplify the system" (the suggested content model YAML) and "The information
architecture should be obvious, not clever" (content types table), content/case-studies/_index.md
(the shape being replaced), CLAUDE.md's Case Study Content Format section.

The plan's four content types: work (evidence-validated product narratives), essays (cornerstone and
practice point-of-view pieces), notes (concise field notes from practice), plus about. Open tools
(the generated /skills/ trees) stay as-is and are not a Hugo collection.

DELIVERABLES:

1. Three archetypes with the validated frontmatter schema:
   - archetypes/work.md — fields: title, type: work, date, summary, status (draft|published),
     evidence_status (needs-verification|verified|range-only|mechanism-only), visibility
     (private|public), employer_review (pending|cleared|n-a), featured (bool), role, users (list),
     themes (list from the small controlled taxonomy: expert-workflows, product-judgment,
     reliable-ai-systems), canonical_url. Include the plan's 8-part body skeleton as comments or
     headings: BLUF / Who was doing the work / What was already possible / The product boundary /
     The hard choice / Athan's ownership / What changed / What he would change now.
   - archetypes/essay.md — title, type: essay, date, tier (cornerstone|practice), summary, status,
     visibility, themes, canonical_url, reading time handled by the template not frontmatter.
   - archetypes/note.md — title, type: note, date, summary, status, visibility, themes,
     source_url (for notes migrated from the Agency site).
   Defaults in every archetype must be the SAFE ones: status: draft, visibility: private,
   evidence_status: needs-verification, employer_review: pending. Nothing is publishable by accident.

2. Section index pages, each with a real _index.md that the Wave 1 templates will render:
   - content/work/_index.md — title "Work", the section's job per the plan (evidence-validated case
     studies and selected product systems). Cascade the safe defaults to children.
   - content/essays/_index.md — part of the Thinking destination.
   - content/notes/_index.md — Field Notes. This is where the migrated Agency material lands.
   Use Hugo's `cascade` in these index files so a child that forgets a field still inherits
   status: draft / visibility: private.

3. config/_default/hugo.toml — the build exclusion rule the plan demands: "The build should exclude
   anything that is not explicitly status: published and visibility: public." Hugo has no native
   frontmatter-predicate exclusion, so implement it the way that actually works and document your
   choice in a comment:
     - the cascade defaults above plus `build: { list: never, render: never }` driven from the unsafe
       states, OR
     - buildDrafts false plus a convention that status != published implies draft: true, enforced in
       the archetypes and asserted by a check script.
   Whichever you pick, it must be true that a file with status: draft or visibility: private produces
   NO output page and NO sitemap entry. Prove it: create a throwaway test file, build to
   /private/tmp/hugo-guard, show it is absent from both the output tree and sitemap.xml, then DELETE
   the throwaway file before you finish.
   Also add the taxonomy for `themes` alongside the existing tag/category taxonomies.
   Do not change baseURL or publishDir.

4. .planning/CONTENT-MODEL.md (new) — one page documenting the four content types, every frontmatter
   field with its allowed values, the evidence_status ladder mapped to the plan's four claim
   classifications (public-and-verified / publishable-in-a-range / publishable-as-mechanism-only /
   private), the exclusion mechanism you chose and why, and the exact command to preview drafts
   locally (`hugo server -D` and what it does and does not reveal).

NOTE: w0-02 also edits config/_default/hugo.toml (sitemap/quarantine) and runs before you. Read the
current file state rather than assuming; do not revert its changes.
