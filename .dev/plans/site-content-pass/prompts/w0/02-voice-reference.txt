REPO: /Users/adial/GitHub/athan-dial.github.io  (Hugo 0.154.3+extended, branch redesign/editorial-systems)

READ FIRST, IN THIS ORDER:
  1. CLAUDE.md — repo rules. Its Content Authenticity Constraint and Employer Safety Guidelines are
     binding on everything you write here.
  2. .planning/CONTENT-SAFETY-CONTRACT.md — the abstraction and claim-labelling rules. Non-negotiable.
  3. .planning/VOICE-REFERENCE.md — how Athan actually writes.
  4. .planning/EVIDENCE-DECISIONS.md — what is settled, retired, and viable (public-safe summary).
  5. .planning/design/REDESIGN-PLAN.md — the strategy this content has to substantiate.
  6. .planning/CONTENT-MODEL.md + archetypes/ — the frontmatter contract you must match exactly.

THE EVIDENCE LIVES IN .planning/private/ (gitignored, on disk, read it):
  - CHATGPT-EVIDENCE-RESPONSE.md — the primary source. Claim-by-claim, with epistemic labels
    (MEASURED / ESTIMATED / TARGET / INFERRED) and sensitivity labels (PUBLIC / INTERNAL /
    LIKELY-CONFIDENTIAL). This is your factual authority.
  - EVIDENCE-LEDGER.md, WORK-CANDIDATES.md, CONTEXT-PACK.md — the earlier internal mining pass.
    Where these disagree with CHATGPT-EVIDENCE-RESPONSE.md, the latter wins: it cites primary
    records, and it corrected several claims the earlier pass had wrong.

THIS REPO IS PUBLIC. That is the single most important fact about your task.
  * NEVER write into content/ any internal program name, tool codename, vendor or CRO name, model
    codename, internal document ID, chat permalink, colleague name, internal hostname, IAM or cloud
    identifier, or absolute dollar figure. The evidence files are full of these. Abstract every one
    per CONTENT-SAFETY-CONTRACT.md.
  * scripts/verify-content-safety.sh enforces this and FAILS THE BUILD. Run it.

CLAIM DISCIPLINE — the reason this plan exists:
  * A `TARGET` is not a result. Every number in the relevant product requirements document is a
    target or success criterion. The quarantined case studies published targets as results; that is
    the specific failure being corrected. If you state a number, it must be MEASURED, and you must
    say what was measured.
  * Where evidence is `NO RECORD`, SAY SO IN THE PROSE. "We did not instrument this, and I would do
    that differently" is a publishable sentence and an on-brand one. Silence is not.
  * Invent nothing. No metric, no date, no quote, no outcome. If a template section has no evidence,
    write the section about the absence or leave it out — do not fill it.

VOICE:
  * Invoke the `writing-style` skill and follow it. This is Athan writing in first person.
  * Athan's global rules: no em-dash pile-ups; no buzzword clusters ("operating layer" as a slogan,
    "decision evidence", "compounds" as a verb); concrete over abstract; one idea per sentence; show
    the real example.
  * Do NOT write like a case-study template being filled in. The headings are scaffolding; the prose
    has to read like a person who was there.

FRONTMATTER — every file you create:
  status: draft
  visibility: private
  These are MANDATORY and must not be changed to published/public. Nothing in this plan ships.
  Athan reviews in local preview first. verify-build.sh exits 5 if you get this wrong.

Do NOT commit. Do NOT push. Minimal focused diff — create or edit ONLY the file(s) this brief names.

GUARD — run before reporting done:
  bash scripts/verify-build.sh

REPORT: the file you wrote, every claim you made with its epistemic label and where you got it,
every place you chose to state an absence, and anything you could not evidence.

TASK: Write .planning/VOICE-REFERENCE.md — a PUBLIC-SAFE reference for how Athan actually writes,
which all four drafting tracks will consume.

SOURCE: .planning/private/CHATGPT-EVIDENCE-RESPONSE.md, Part 4 "Voice corpus". It contains 9 verbatim
excerpts of Athan's own writing (unedited, typos retained) with audience and format labels, plus a
table of 9 recurring constructions with verbatim examples.

THE CONSTRAINT THAT SHAPES THIS FILE: the excerpts are labelled LIKELY-CONFIDENTIAL — they are real
internal messages naming real programs, tools, vendors, and colleagues. You are writing a file that
will be COMMITTED TO A PUBLIC REPO. So:
  * Do NOT reproduce the excerpts verbatim.
  * Do NOT quote any sentence that contains an internal program name, tool name, vendor name, or
    colleague name.
  * DO describe the observable patterns precisely enough to be usable, and where you need an example,
    either construct a neutral one in the same shape or quote only a fragment that carries no
    internal detail (e.g. an opener, a connective, a hedge).
  * Point drafting agents at .planning/private/ for the verbatim corpus — they have it on disk.

DELIVERABLES — .planning/VOICE-REFERENCE.md containing:

1. The 9 recurring constructions, each with: what it is, when he reaches for it, and a neutral
   example in the same shape. Several are safe to quote as bare fragments because they contain
   nothing internal — judge each one.

2. Structural habits visible across the corpus. Derive these yourself from reading it; do not just
   restate my list. Things to look for: where the conclusion sits relative to the evidence; how he
   marks uncertainty; whether he hedges before or after committing to a view; how he handles
   disagreement with a colleague's framing; how he compresses for executives versus peers; what he
   does with lists and arrows; how he closes.

3. What he does NOT do — the negative patterns, which are as useful as the positive ones. Look for
   absence of: corporate throat-clearing, superlatives, tricolons, "I'm excited to share",
   rhetorical questions as transitions, and conclusions that restate the opening.

4. Channel calibration: the corpus spans Slack DMs, channel posts, exec updates, and a public post.
   Say how the register shifts, since the drafting tracks are writing for a public site — the closest
   register in the corpus is the public post, and the exec-summary compression is the closest thing
   to how a case-study BLUF should read.

5. A short "if you only remember three things" section at the top, because that is what the drafting
   agents will actually act on.

6. One honest correction to record: "metric theater" is NO RECORD as a recurring phrase in the
   validated work corpus. It appears in current site copy and was previously assumed to be a
   signature phrase. It is a real phrase Athan uses, but it is not load-bearing — the 9 documented
   constructions are. Say so plainly so no drafting agent over-indexes on it.

Also invoke the `writing-style` skill and reconcile it with what you observe in the corpus. Where the
skill and the primary corpus disagree, the corpus wins and you should note the discrepancy — the
skill is a prior, the corpus is evidence.

Do NOT write into content/. This PR produces one documentation file.
